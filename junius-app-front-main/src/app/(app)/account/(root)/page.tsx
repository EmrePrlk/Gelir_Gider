import AccountView from 'src/app/(app)/account/_views/account-view/account-view';

export const metadata = {
  title: 'Junius App: Account',
};

export default function AccountPage() {
  return <AccountView />;
}
